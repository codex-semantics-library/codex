struct pair { 
  int a ;
  int b ;
};

struct list { 
  int a ;
  struct list * next ;
};

struct list + alloc_list() ;
struct pair + alloc_pair() ;

((int with self > 1) with self < 4) test_pair(struct pair + p);
