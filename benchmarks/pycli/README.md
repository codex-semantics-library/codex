# Pycli library

This folder contains a small python library used by the `../manage.py` script.
It is mostly centered around helpers to create pretty terminal displays.
It requires python > 3.10 for the lightweight type annotation syntax.
